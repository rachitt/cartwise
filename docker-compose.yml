services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: cartwise
      POSTGRES_HOST_AUTH_METHOD: trust
    ports:
      - "5432:5432"
    volumes:
      - cartwise-postgres:/var/lib/postgresql/data

volumes:
  cartwise-postgres:
